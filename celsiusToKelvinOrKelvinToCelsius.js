const celsiusToKelvinOrKelvinToCelsius = temp => (!/[ck]/i.test(temp[temp.length - 1]) || /[ck]/i.test(temp[0]) || temp[0] === ' ') ? new Error('invalid input; the valid format is #C (or #c) or #K (or #k).') : (/c/i.test(temp) ? ((temp.replace(/c/i, '') - (-273.15)) + ' °K') : ((temp.replace(/k/i, '') - 273.15) + ' °C'));